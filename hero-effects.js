const s=document.querySelector('.symbol');let x=0,y=0,tx=0,ty=0;addEventListener('pointermove',e=>{tx=(e.clientX/innerWidth-.5);ty=(e.clientY/innerHeight-.5)},{passive:true});function frame(t){x+=(tx-x)*.035;y+=(ty-y)*.035;const float=Math.sin(t*.00045)*5;s.style.left='calc(56% + '+(x*7)+'px)';s.style.marginTop=float+'px';s.style.setProperty('--ry',(x*2.2)+'deg');s.style.setProperty('--rx',(-y*1.15)+'deg');requestAnimationFrame(frame)}if(!matchMedia('(prefers-reduced-motion: reduce)').matches)requestAnimationFrame(frame);