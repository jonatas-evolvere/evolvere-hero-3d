const s=document.querySelector('.symbol');let x=0,y=0,tx=0,ty=0;const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;addEventListener('pointermove',e=>{tx=e.clientX/innerWidth-.5;ty=e.clientY/innerHeight-.5},{passive:true});function frame(t){x+=(tx-x)*.025;y+=(ty-y)*.025;const mobile=innerWidth<621;const amp=mobile?1.2:2.4;s.style.marginTop=(Math.sin(t*.00048)*(mobile?2:4))+'px';s.style.setProperty('--ry',(x*amp)+'deg');s.style.setProperty('--rx',(-y*amp*.55)+'deg');requestAnimationFrame(frame)}if(!reduced)requestAnimationFrame(frame);