const s=document.querySelector('.symbol');let x=0,y=0,tx=0,ty=0;const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;addEventListener('pointermove',e=>{if(innerWidth<=620)return;tx=e.clientX/innerWidth-.5;ty=e.clientY/innerHeight-.5},{passive:true});function frame(t){x+=(tx-x)*.018;y+=(ty-y)*.018;const mobile=innerWidth<=620;const amp=mobile?0:2.6;s.style.setProperty('--ry',(x*amp)+'deg');s.style.setProperty('--rx',(-y*amp*.55)+'deg');requestAnimationFrame(frame)}if(!reduced)requestAnimationFrame(frame);